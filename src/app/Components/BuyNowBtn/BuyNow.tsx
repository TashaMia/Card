import styles from "../../Styles/buttons.module.css";

export default function BuyNow() {
  return (
    <button id="test2" className={`${styles.btn} ${styles.buyBtn}`}>
      Buy now
    </button>
  );
}
